<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xsi:schemaLocation="http://www.springframework.org/schema/rabbit
           https://www.springframework.org/schema/rabbit/spring-rabbit.xsd
           http://www.springframework.org/schema/beans
           https://www.springframework.org/schema/beans/spring-beans.xsd">

  <rabbit:connection-factory id="connectionFactory" virtual-host="/" host="${spring.rabbitmq.host}"
                             password="${spring.rabbitmq.password}"
                             port="${spring.rabbitmq.port}"
                             confirm-type="SIMPLE"
  />

  <rabbit:admin connection-factory="connectionFactory"/>

  <rabbit:queue name="auth.access-log.queue" auto-declare="true"/>
  <rabbit:direct-exchange name="auth.access-log.exg" auto-declare="true">
    <rabbit:bindings>
      <rabbit:binding key="auth.access-log.save"
                      queue="auth.access-log.queue"></rabbit:binding>
    </rabbit:bindings>
  </rabbit:direct-exchange>

  <rabbit:queue name="auth.operate-log.queue" auto-declare="true"/>
  <rabbit:direct-exchange name="auth.operate-log.exg" auto-declare="true">
    <rabbit:bindings>
      <rabbit:binding key="auth.operate-log.save"
                      queue="auth.operate-log.queue"></rabbit:binding>
    </rabbit:bindings>
  </rabbit:direct-exchange>

  <rabbit:listener-container connection-factory="connectionFactory">
    <rabbit:listener ref="accessLogSaveListener" queue-names="auth.access-log.queue"></rabbit:listener>
    <rabbit:listener ref="operateLogSaveListener" queue-names="auth.operate-log.queue"></rabbit:listener>
  </rabbit:listener-container>

</beans>